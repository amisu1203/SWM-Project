// $(document).ready(() => {
//   show_user();
// });

// const show_user = () => {
//   fetch("/user_list")
//     .then((res) => {
//       console.log(res);
//       return res.json();
//     })
//     .then((data) => {
//       console.log(data);

//       //   const loggedInUser = data["users"][0]; // Assuming only one user is returned
//       //   const username = loggedInUser.name;
//       //   console.log("Logged in user: ", username);
//       // You can now use the username variable to do whatever you want with the logged-in user's name.
//     });
// };
